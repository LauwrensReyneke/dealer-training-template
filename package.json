{
  "name": "templating-engine",
  "version": "1.0.1",
  "description": "Dealer template editor (Vue + Express + sql.js)",
  "main": "index.js",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm install --prefix client && npm --prefix client run build",
    "preview": "npm --prefix client run preview",
    "serve": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "sql.js": "^1.13.0"
  },
  "private": true
}
